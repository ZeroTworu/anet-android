<?xml version="1.0" encoding="utf-8"?>
<transition xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Первый drawable (начальное состояние) -->
    <item android:drawable="@drawable/button_gradient_connect" />
    <!-- Второй drawable (конечное состояние) -->
    <item android:drawable="@drawable/button_gradient_disconnect" />
</transition>